# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty. The only tracked file besides this one is `README.md`, which contains a single-line title (`# job`) and no further content. There is no source code, build system, dependency manifest, test suite, lint configuration, CI workflow, or AI-assistant rules (no `.cursor/rules/`, no `.cursorrules`, no `.github/copilot-instructions.md`) to draw guidance from yet.

As a result, there are no project-specific build, lint, test, or run commands to document at this time, and no architecture to summarize.

## Working in this repository

- Default development branch for AI-generated changes: `claude/add-claude-documentation-bi1mk` (the branch this file was created on). Confirm the intended branch with the user before pushing further work.
- When the first real code is added (language, framework, package manager, test runner, etc.), update this file with:
  - The actual build / lint / test / run commands, including how to run a single test.
  - A "big picture" architecture section covering structure that spans multiple files.
  - Any conventions established in a README, Cursor rules, or Copilot instructions once they exist.
